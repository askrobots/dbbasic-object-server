{
  "name": "shell_preferences",
  "title": "Shell Preferences",
  "version": 6,
  "description": "Per-user shell settings; the record id is the user id.",
  "fields": [
    {
      "name": "id"
    },
    {
      "name": "ai_model",
      "type": "text",
      "label": "AI Model",
      "default": "anthropic:claude-sonnet-5",
      "help": "service:model, like anthropic:claude-haiku-4-5 or openai:gpt-5-mini"
    },
    {
      "name": "tools",
      "type": "text",
      "label": "AI Tools",
      "default": "global_search,list_records,get_record,create_record",
      "help": "Comma-separated MCP tool names offered to the model"
    },
    {
      "name": "voice_enabled",
      "type": "boolean",
      "label": "Voice",
      "default": "false",
      "help": "Speak AI replies aloud and enable the mic button (/voice toggles)"
    },
    {
      "name": "talk_wake_word",
      "type": "text",
      "label": "Talk Wake Word",
      "default": "computer",
      "help": "Talk page: word that arms conversation-mode capture; empty captures everything while the mic is on"
    },
    {
      "name": "talk_end_word",
      "type": "text",
      "label": "Talk End Word",
      "default": "over",
      "help": "Talk page: trailing word that ends and submits an utterance; empty disables end-word submission"
    },
    {
      "name": "talk_endpoint",
      "type": "enum",
      "label": "Talk Endpoint Mode",
      "default": "silence",
      "enum": [
        "silence",
        "word",
        "manual"
      ],
      "help": "How Talk decides an utterance is finished: silence (VAD), word (end word only), or manual (button/Enter only)"
    },
    {
      "name": "talk_silence_ms",
      "type": "text",
      "label": "Talk Silence Endpoint (ms)",
      "default": "1400",
      "help": "Talk page: milliseconds of sustained quiet that ends an utterance in silence endpoint mode"
    },
    {
      "name": "talk_tts",
      "type": "enum",
      "label": "Voice Engine",
      "default": "auto",
      "enum": [
        "auto",
        "server",
        "browser"
      ],
      "help": "Which engine speaks AI replies: auto prefers an on-device browser voice when one is available and falls back to the server otherwise, server always uses server TTS (with a browser fallback on failure), browser always uses the browser's own speechSynthesis"
    },
    {
      "name": "talk_tts_engine",
      "type": "enum",
      "label": "Server Voice Quality",
      "default": "local",
      "enum": [
        "local",
        "openai"
      ],
      "help": "Ignored if Voice Engine is set to browser (that always uses your browser's own voice). Under server or auto, local is free and always available (espeak-ng in production); openai is a real cloud voice, clearer and more natural -- choosing it overrides auto's browser-voice preference too, so you reliably hear it -- but costs real money per reply and requires an OpenAI key under your account's service keys"
    },
    {
      "name": "talk_stt_engine",
      "type": "enum",
      "label": "Speech Recognition",
      "default": "browser",
      "enum": [
        "browser",
        "openai"
      ],
      "help": "browser uses your browser's built-in speech recognition (free, but silently unavailable on some browsers, e.g. Firefox); openai records real audio and transcribes it on the server via your own OpenAI key -- costs a small real amount per utterance, and only supports push-to-talk (tap to start, tap to stop), not continuous wake-word listening"
    },
    {
      "name": "talk_model",
      "type": "text",
      "help": "Model for the Talk surface only; blank falls back to ai_model. Exists because voice tolerates latency far worse than a terminal: a fast model that answers in 3 seconds beats a smarter one that leaves 10 seconds of silence."
    }
  ],
  "forms": {
    "default": {
      "fields": [
        "ai_model",
        "tools",
        "voice_enabled",
        "talk_wake_word",
        "talk_end_word",
        "talk_endpoint",
        "talk_silence_ms",
        "talk_tts",
        "talk_tts_engine",
        "talk_stt_engine"
      ]
    }
  },
  "views": {
    "list_mode": "table",
    "list_fields": [
      "ai_model",
      "tools",
      "voice_enabled",
      "talk_endpoint"
    ]
  }
}
