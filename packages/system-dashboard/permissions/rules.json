{
  "rules": [
    {
      "effect": "allow",
      "principal": "role:admin",
      "actions": ["read"],
      "collection": "scheduler_runs",
      "reason": "scheduled-run history is an admin inspection surface (and drives the /scheduler page's live feed); rows are written server-side by the daemon, never by a client"
    },
    {
      "effect": "allow",
      "principal": "public",
      "actions": ["execute"],
      "object_id": "system_scheduler",
      "reason": "the scheduler page is public execute like every page object; it renders a sign-in/admin prompt to everyone else and gates all data and every POST action on an admin identity itself"
    }
  ]
}
