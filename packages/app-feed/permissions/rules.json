{
  "rules": [
    {
      "effect": "allow",
      "principal": "public",
      "actions": ["execute"],
      "object_id": "site_feed",
      "reason": "the feed page itself is publicly reachable; it shows a sign-in prompt to anonymous visitors and gates the actual composed read on identity via GET /api/feed (object_server.py's _handle_feed returns an empty, non-error result for an anonymous caller), same posture as app-dashboard's site_dashboard"
    }
  ]
}
