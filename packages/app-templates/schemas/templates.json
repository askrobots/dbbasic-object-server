{
  "name": "templates",
  "title": "Templates",
  "version": 4,
  "description": "Reusable, structured-data templates. `body` remains a free-text fallback from v1; a private predecessor-system audit (not part of this repo) showed templates there are chiefly a JSON Schema (`schema`) plus optional AI-assist config, so v2 adds those fields alongside it. Edit history comes from the record changelog.",
  "fields": [
    {
      "name": "id"
    },
    {
      "name": "name",
      "type": "text",
      "required": true,
      "label": "Name",
      "validation": {
        "min_length": 1,
        "max_length": 120
      }
    },
    {
      "name": "description",
      "type": "text",
      "label": "Description",
      "validation": {
        "max_length": 300
      }
    },
    {
      "name": "category",
      "type": "text",
      "label": "Category"
    },
    {
      "name": "body",
      "type": "textarea",
      "required": true,
      "label": "Body",
      "validation": {
        "min_length": 1
      }
    },
    {
      "name": "schema",
      "type": "textarea",
      "label": "Schema (JSON)",
      "help": "JSON Schema describing this template's structured fields -- the core of a template on the audited predecessor. Stored as a JSON string, same convention as invoice_lines/invoices use plain typed fields and this codebase's field-type contract (docs/schema-forms.md) has no dedicated json type. Kept optional (not required) so v1 template records saved before this field existed stay valid to read and re-save."
    },
    {
      "name": "ui_schema",
      "type": "textarea",
      "label": "UI Schema (JSON)",
      "help": "Optional form-presentation hints for `schema` above, e.g. field order or widget choice. JSON string, same storage convention as `schema`."
    },
    {
      "name": "instructions",
      "type": "textarea",
      "label": "Instructions",
      "help": "Human-readable how-to for whoever fills out this template."
    },
    {
      "name": "ai_prompt",
      "type": "textarea",
      "label": "AI Prompt",
      "help": "Instructions an AI assistant would use to help fill this template. Stored config only -- this codebase has no AI auto-fill engine; nothing reads or executes this field yet."
    },
    {
      "name": "ai_assistance",
      "type": "enum",
      "label": "AI Assistance",
      "default": "suggestions",
      "enum": [
        "none",
        "suggestions",
        "auto_fill",
        "full"
      ],
      "help": "How much AI help this template calls for, mirroring the audited predecessor's levels. Stored preference only -- deferred infra, same posture as ai_prompt."
    },
    {
      "name": "handler",
      "type": "text",
      "label": "Run Handler",
      "help": "Which engine executes this template when somebody presses Run: 'echo' renders the body with the form values and returns it (free, no provider); 'ai_text' sends the rendered body to a configured AI provider. Empty means this template is a document, not a job -- action_run_template refuses it by name. Text rather than an enum so a new handler is a new runner capability, not a schema migration of every install."
    },
    {
      "name": "run_cost_cents",
      "type": "integer",
      "default": "0",
      "label": "Run Price",
      "help": "What one run costs the person running it, in integer cents (doctrine #10). This is a PRICE, not a passthrough of the provider's bill: it is stamped onto the run at queue time and held against the wallet before anything executes, so editing it next week changes what the NEXT run costs and restates nothing anybody already paid -- the same act as stamping a rate on a time entry (docs/logic-decisions.md #1). What the run costs US at the provider is recorded on the run separately, so margin is a fold rather than a mystery. 0 means free to run, and free runs touch the wallet not at all."
    },
    {
      "name": "default_values",
      "type": "textarea",
      "label": "Default Values (JSON)",
      "help": "Values pre-filled into `schema`'s fields when starting from this template. JSON string, same storage convention as `schema`."
    },
    {
      "name": "example_data",
      "type": "textarea",
      "label": "Example Data (JSON)",
      "help": "A worked example matching `schema`, shown as a sample rather than used as a default. JSON string, same storage convention as `schema`."
    },
    {
      "name": "tags",
      "type": "text",
      "label": "Tags",
      "help": "Comma-separated tag names"
    },
    {
      "name": "is_public",
      "type": "boolean",
      "label": "Public",
      "default": "false"
    },
    {
      "name": "created_at",
      "type": "datetime",
      "read_only": true,
      "label": "Created"
    },
    {
      "name": "owner_id",
      "type": "text",
      "label": "Owner"
    }
  ],
  "forms": {
    "default": {
      "fields": [
        "name",
        "description",
        "category",
        "body",
        "schema",
        "instructions",
        "ai_prompt",
        "ai_assistance",
        "default_values",
        "tags",
        "is_public"
      ]
    }
  },
  "views": {
    "list_mode": "table",
    "list_fields": [
      "name",
      "category",
      "ai_assistance",
      "is_public"
    ]
  },
  "search": {
    "fields": [
      "name",
      "description",
      "body",
      "instructions",
      "tags"
    ]
  }
}
