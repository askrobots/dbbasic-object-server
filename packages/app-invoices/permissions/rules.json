{
  "rules": [
    {
      "effect": "allow",
      "principal": "registered",
      "actions": ["create", "read", "update", "delete"],
      "collection": "invoices",
      "row_filter": {"owner_id": "$user_id"},
      "reason": "signed-in users manage invoices, quotes, and credit notes they created"
    },
    {
      "effect": "allow",
      "principal": "registered",
      "actions": ["create", "read", "update", "delete"],
      "collection": "invoice_lines",
      "row_filter": {"owner_id": "$user_id"},
      "reason": "signed-in users manage line items on documents they created"
    },
    {
      "effect": "allow",
      "principal": "public",
      "actions": ["execute"],
      "object_id": "site_invoices",
      "reason": "the invoices page is public; it shows a sign-in prompt to visitors"
    },
    {
      "effect": "allow",
      "principal": "public",
      "actions": ["execute"],
      "object_id": "site_invoice_view",
      "reason": "the invoice detail page is public; the row-filtered collection API still decides which invoice is actually returned"
    }
  ]
}
