{
  "rules": [
    {
      "effect": "allow",
      "principal": "registered",
      "actions": ["create", "read", "update", "delete"],
      "collection": "entities",
      "row_filter": {"owner_id": "$user_id"},
      "reason": "signed-in users manage their own entities (sets of books); books are private to their owner, no public read"
    }
  ]
}
