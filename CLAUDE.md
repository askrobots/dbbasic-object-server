See [`AGENTS.md`](AGENTS.md).

It is the entry point for any AI agent working on this repository — the
doctrine, the traps that fail silently, the refusals that are decisions
rather than gaps, and where to read next. Kept in one file under the
cross-tool name so every agent finds the same guidance.
